{
  "name": "page-tui",
  "version": "0.1.0",
  "description": "A small, dependency-free Node.js TUI framework with page-stack navigation.",
  "private": true,
  "main": "src/index.js",
  "type": "commonjs",
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build-sea.js",
    "build:sea": "node scripts/build-sea.js",
    "start": "node src/loader.js",
    "test": "node --test",
    "test:extension": "npm --prefix vscode-extension test",
    "package:extension": "npm --prefix vscode-extension run package"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
